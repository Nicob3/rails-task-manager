<h2><%=@task.title%></h2>
<p><%=@task.details%></p>
<p><%=@task.completed.to_s%></p>
 <%= link_to "Back to list", tasks_path %>
 <%= link_to "Remove task", task_path(@task),
 data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
 %>
